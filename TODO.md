# TODO

- [ ] Perbaiki `TicketController` (nama class harus konsisten)
- [ ] Pastikan controller admin kategori konsisten dengan route (`CategoriesController` vs `CategoryController`)
- [ ] Perbaiki `resources/views/admin/dashboard.blade.php` agar menggunakan data dinamis dari controller
- [x] Perbaiki `TicketController` (nama class konsisten)
- [x] Pastikan controller admin kategori konsisten dengan route
- [x] Perbaiki `resources/views/admin/dashboard.blade.php` supaya pakai data dari controller
- [x] Cek `routes/web.php` agar tidak mengarah ke controller yang tidak ada
- [ ] (Opsional) Perbaiki `AdminController` / routing admin yang belum sinkron jika fitur admin events/kategori lain tidak berfungsi
- [ ] Jalankan verifikasi cepat: `php artisan route:list` dan cek halaman `/admin/dashboard`


